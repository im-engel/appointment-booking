const API_URL = "http://localhost:9000"
const LOCAL_HOST = "http://localhost:1234"

const SORT_OPTIONS = [
    "Clinician Name",
    "Date"
]

export default {
    API_URL,
    LOCAL_HOST,
    SORT_OPTIONS
}